# ==============================================
# 1. Godot 4.x 官方强制忽略规则（核心必选）
# ==============================================
# Godot编辑器自动生成的核心缓存文件夹
# 【本质】：存放资源导入缓存、编辑器状态、临时编译文件、渲染缓存、场景预览图，每次打开编辑器都会自动重新生成
# 【为什么忽略】：
# 1. 完全可复现：只要有项目源文件，打开Godot就会自动生成，完全不需要提交
# 2. 卡死元凶：内部包含上千个随时变化的小文件，是Trae Git卡死的头号原因
# 3. 避免冲突：和本地编辑器环境强绑定，每个人的电脑生成的内容都不一样，提交会导致无限冲突
# 【官方依据】：Godot 4.x官方文档明确要求必须忽略该文件夹
.godot/

# 自动生成的二进制翻译文件，由CSV源文件编译生成
# 【本质】：Godot从翻译CSV源文件自动生成的二进制文件，可100%复现
# 【为什么忽略】：源CSV文件已经纳入版本管理，这个生成文件不需要重复提交
*.translation

# 平台相关的原生编译库文件
# 【本质】：C#/C++扩展编译生成的Windows/Mac/Linux平台原生库文件
# 【为什么忽略】：可从源码重新编译生成，不需要提交到仓库，避免仓库体积爆炸
*.so
*.dll
*.dylib

# ==============================================
# 2. 适配你项目架构规范的忽略规则
# ==============================================
# 项目存档文件夹（你的架构规范里定义的持久化存档目录）
# 【本质】：玩家本地的游戏存档、设置持久化文件，和单个玩家的本地环境绑定
# 【为什么忽略】：
# 1. 符合架构规范：存档读写由全局_数据管理器统一管理，本地测试存档不需要提交到仓库
# 2. 避免冲突：每个开发窗口的本地存档都不一样，提交会导致数据冲突
# 3. 数据安全：避免把测试存档、敏感配置提交到远程仓库
项目存档/

# 游戏导出文件夹
# 【本质】：打包游戏生成的exe、apk、pck等导出文件
# 【为什么忽略】：可从源码重新导出，文件体积极大，提交会导致仓库爆炸，完全不需要版本管理
导出/
Build/
build/
*.exe
*.apk
*.ipa
*.pck
*.zip

# ==============================================
# 3. 解决Trae卡死的专属忽略规则
# ==============================================
# Trae AI开发环境的临时缓存文件夹
# 【本质】：Trae自动生成的AI上下文缓存、临时文件、进程缓存
# 【为什么忽略】：和Trae的本地运行环境绑定，完全可复现，是Trae Git卡死的第二大元凶
.trae/
.ai/
*.ai_tmp
*.trae_cache
.traeignore

# ==============================================
# 4. 大文件/源文件忽略规则（避免仓库体积爆炸）
# ==============================================
# 美术/音频源文件，不需要纳入版本管理
# 【本质】：PSD、AI工程文件、未压缩的WAV音频等，是资源的原始工程文件
# 【为什么忽略】：文件体积极大，不需要提交到代码仓库，用云盘/素材库管理即可
*.psd
*.ai
*.aep
*.fla
*.wav
*.mp4
*.ogg

# ==============================================
# 5. 系统/编辑器临时文件忽略规则
# ==============================================
# Windows/Mac系统自动生成的缓存文件
.DS_Store
Thumbs.db

# 代码编辑器的本地配置文件
# 【本质】：VSCode、Rider等编辑器的个人本地配置，和开发环境绑定
# 【为什么忽略】：每个人的编辑器配置都不一样，提交会导致团队冲突，不需要版本管理
.vscode/
.idea/
.vs/

# 临时/备份/日志文件
# 【本质】：开发过程中生成的临时备份、日志文件
# 【为什么忽略】：无业务意义，不需要版本管理，避免冗余提交
*.tmp
*.temp
*.log
*.bak
*.swp
*.patch
*.diff